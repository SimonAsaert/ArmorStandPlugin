package tld.sima.armorstand.utils;

public enum ToolType {
	// When defined as 'OTHER', let other plugins take over.
	NONE, GRAVITY, SIZE, INVISIBLE, FIRE, BASE, ARMS, GLOW, NAME, ROTATION, MOVE, OTHER;
}
